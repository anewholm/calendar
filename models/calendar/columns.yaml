columns:
    id:
        label: id
        type: number
    name:
        label: name
        type: text
    owner_user[first_name]:
        label: Owner
        type: text
    owner_user_group[name]:
        label: Owner Group
        type: number
    permissions:
        label: permissions
        type: number
    created_at:
        label: created_at
        type: datetime
    updated_at:
        label: updated_at
        type: datetime
