columns:
    id:
        label: id
        type: number
    name:
        label: name
        type: text
    description:
        label: description
        type: text
    created_at:
        label: created_at
        type: datetime
    updated_at:
        label: updated_at
        type: datetime
    owner_user_id:
        label: owner_user_id
        type: number
    owner_user_group_id:
        label: owner_user_group_id
        type: number
    permissions:
        label: permissions
        type: number
