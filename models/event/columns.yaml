columns:
    id:
        label: id
        type: number
    calendar_id:
        label: calendar_id
        type: number
    owner_user_id:
        label: owner_user_id
        type: number
    owner_user_group_id:
        label: owner_user_group_id
        type: number
    created_at:
        label: created_at
        type: datetime
    updated_at:
        label: updated_at
        type: datetime
    permissions:
        label: permissions
        type: number
